import { apiRoutes } from "@testcenter-rewrite/contracts";
import { fixtureCatalog } from "@testcenter-rewrite/test-fixtures";

export const spikeAcceptanceChecks = [
  `npm run db:migrate applies tracked schema versions before API, dispatcher, notification-service, or worker startup`,
  `GET ${apiRoutes.platformHealth} returns a structured health response`,
  `POST ${apiRoutes.platformTenants} creates a tenant with tenant-scoped identity`,
  `GET ${apiRoutes.tenantActivationPolicy("demo-tenant")} returns the tenant default activation-policy flags`,
  `PATCH ${apiRoutes.tenantActivationPolicy("demo-tenant")} updates the tenant default activation policy for future workspaces`,
  `GET ${apiRoutes.tenantOperationalPolicy("demo-tenant")} returns the tenant default operational-policy values for dispatcher timing and worker maintenance grace`,
  `PATCH ${apiRoutes.tenantOperationalPolicy("demo-tenant")} updates the tenant default operational policy for inheriting workspaces`,
  `GET ${apiRoutes.tenantLaunchApprovalPolicy("demo-tenant")} returns the tenant default launch-approval policy values for warning-level approval expiry`,
  `PATCH ${apiRoutes.tenantLaunchApprovalPolicy("demo-tenant")} updates the tenant default launch-approval policy for inheriting workspaces`,
  `GET ${apiRoutes.tenantNotificationPolicy("demo-tenant")} returns the tenant default notification-policy values for breach-notification channel selection plus provider-specific webhook/email retry delay and max-attempt rules`,
  `PATCH ${apiRoutes.tenantNotificationPolicy("demo-tenant")} updates the tenant default provider-specific notification policy for inheriting workspaces`,
  `GET ${apiRoutes.tenantNotificationProviderProfiles("demo-tenant")} returns the tenant-managed notification provider-profile registry for outbound delivery targets together with enable/disable state, rollout state, rolloutPercentage, fallback-profile routing, target-probe policy, masked credential-reference metadata, computed credentials/health/probe status, and provider-operations-service-refreshed operational state including active probe target and latency details`,
  `PATCH ${apiRoutes.tenantNotificationProviderProfiles("demo-tenant")} updates the tenant-managed notification provider-profile registry used by profile-based outbound delivery targets while validating raw credential references, rollout-state values, rollout fallbacks, and target-probe policy on write and preserving existing operational state only for unchanged profile config until the provider-operations service refreshes it again with active probe results`,
  `GET ${apiRoutes.tenantEvidenceRetentionPolicy("demo-tenant")} returns the tenant default evidence-retention policy values for workspace-review and operator-investigation evidence windows`,
  `PATCH ${apiRoutes.tenantEvidenceRetentionPolicy("demo-tenant")} updates the tenant default evidence-retention policy for inheriting workspaces across both retention classes`,
  `GET ${apiRoutes.tenantEvidenceRetentionClassPolicy("demo-tenant")} returns the tenant default evidence-retention class registry, including the tenant-defined hold-reason catalog with display labels, workflow hints, severity, escalation targets, UI grouping, acknowledgement requirements, default assignee targets, and SLA timers that maps retention classes onto TTL policy fields and hold transitions`,
  `PATCH ${apiRoutes.tenantEvidenceRetentionClassPolicy("demo-tenant")} updates the tenant default evidence-retention class registry and hold-reason catalog metadata for inheriting workspaces`,
  `GET ${apiRoutes.tenantPolicyHistory("demo-tenant")} projects tenant activation, operational, launch-approval, notification, notification-provider-profile, evidence-retention, and evidence-retention-class policy changes from audit history into a normalized timeline`,
  `POST ${apiRoutes.tenantWorkspaces("demo-tenant")} creates a workspace inside the selected tenant`,
  `GET ${apiRoutes.workspaceActivationPolicy("demo-tenant", "demo-workspace")} returns workspace activation-policy mode, tenant defaults, flattened override values, per-field override records with provenance, and the effective policy`,
  `PATCH ${apiRoutes.workspaceActivationPolicy("demo-tenant", "demo-workspace")} can switch a workspace between live inheritance and rule-specific override records for release guardrail behavior`,
  `existing inheriting workspaces reflect tenant activation-policy changes live, while overridden workspaces keep only their explicitly overridden fields and continue inheriting the rest`,
  `new workspaces inherit the current tenant activation-policy defaults until they receive a partial field-level override`,
  `GET ${apiRoutes.workspaceOperationalPolicy("demo-tenant", "demo-workspace")} returns workspace operational-policy mode, tenant defaults, flattened override values, per-field override records with provenance, and the effective timing policy`,
  `PATCH ${apiRoutes.workspaceOperationalPolicy("demo-tenant", "demo-workspace")} can switch a workspace between live inheritance and rule-specific override records for dispatcher timing and worker maintenance grace`,
  `GET ${apiRoutes.workspaceLaunchApprovalPolicy("demo-tenant", "demo-workspace")} returns workspace launch-approval-policy mode, tenant defaults, flattened override values, per-field override records with provenance, and the effective approval-expiry policy`,
  `PATCH ${apiRoutes.workspaceLaunchApprovalPolicy("demo-tenant", "demo-workspace")} can switch a workspace between live inheritance and rule-specific override records for warning-level launch-approval expiry`,
  `GET ${apiRoutes.workspaceNotificationPolicy("demo-tenant", "demo-workspace")} returns workspace notification-policy mode, tenant defaults, flattened override values, per-field override records with provenance, and the effective provider-specific breach-notification delivery policy`,
  `PATCH ${apiRoutes.workspaceNotificationPolicy("demo-tenant", "demo-workspace")} can switch a workspace between live inheritance and rule-specific override records for breach-notification channel selection plus provider-specific webhook/email retry delay and max-attempt rules`,
  `GET ${apiRoutes.workspaceNotificationProviderProfiles("demo-tenant", "demo-workspace")} returns workspace notification-provider-profile mode, tenant defaults, flattened partial override values, explicit removed-profile tombstones, per-profile override records with provenance, enable/disable state, rollout state, rolloutPercentage, fallback-profile routing, target-probe policy, masked credential-reference metadata, computed credentials/health/probe status, provider-operations-service-refreshed operational state including target reachability details, and the effective outbound provider-profile registry after live inheritance for untouched profiles`,
  `PATCH ${apiRoutes.workspaceNotificationProviderProfiles("demo-tenant", "demo-workspace")} can switch a workspace between live inheritance and partial per-profile override records plus explicit profile-removal tombstones for outbound delivery targets while validating credential references, rollout-state values, rollout fallbacks, and target-probe policy on write, preserving untouched tenant profiles, carrying forward operational state only for unchanged profiles, and allowing future deliveries to treat paused, unhealthy, or rollout-gated profiles as unavailable or fall back to alternate profiles`,
  `the dedicated provider-operations service refreshes tenant defaults and workspace overrides with lastCheckedAt, actor provenance, rollout status, active credential and target probe results, probe-policy skips, and reachability errors including target_unreachable without rewriting untouched config semantics or flooding the audit stream when nothing changed`,
  `GET ${apiRoutes.workspaceEvidenceRetentionPolicy("demo-tenant", "demo-workspace")} returns workspace evidence-retention-policy mode, tenant defaults, flattened override values, per-field override records with provenance, and the effective class-based evidence retention policy`,
  `PATCH ${apiRoutes.workspaceEvidenceRetentionPolicy("demo-tenant", "demo-workspace")} can switch a workspace between live inheritance and rule-specific override records for class-based system-check evidence retention`,
  `GET ${apiRoutes.workspaceEvidenceRetentionClassPolicy("demo-tenant", "demo-workspace")} returns workspace evidence-retention-class-policy mode, tenant defaults including the inherited hold-reason catalog metadata, flattened partial override values, rule-level override records with provenance, and the effective class registry`,
  `PATCH ${apiRoutes.workspaceEvidenceRetentionClassPolicy("demo-tenant", "demo-workspace")} can switch a workspace between live inheritance and rule-level override records for specific retention-class rules while continuing to inherit the tenant-defined hold-reason catalog and the rest of the registry`,
  `GET ${apiRoutes.workspaceEvidenceRetentionClasses("demo-tenant", "demo-workspace")} returns the effective workspace evidence-retention class registry, including class-specific TTLs and hold-promotion rules enriched with hold-reason labels, workflow hints, severity, escalation targets, UI grouping, acknowledgement requirements, default assignee targets, and SLA timers`,
  `GET ${apiRoutes.workspacePolicyHistory("demo-tenant", "demo-workspace")} projects workspace activation, operational, launch-approval, notification, notification-provider-profile, evidence-retention, and evidence-retention-class policy changes into a normalized timeline with historical default snapshots`,
  `GET ${apiRoutes.workspaceSystemCheckResults("demo-tenant", "demo-workspace")}?groupKey=group-alpha&status=warning&reviewStatus=pending lists persisted participant system-check submissions with summary, evidence metadata, and review-state filtering for workspace review`,
  `GET ${apiRoutes.workspaceSystemCheckReadiness("demo-tenant", "demo-workspace")}?groupKey=group-alpha&readinessStatus=blocked lists participant sessions that are blocked, warning, or ready for launch based on latest system-check outcomes`,
  `GET ${apiRoutes.workspaceSystemCheckEvidenceHoldQueue("demo-tenant", "demo-workspace")}?groupKey=group-alpha&acknowledgementStatus=pending&assignmentStatus=assigned&escalationStatus=escalated lists held evidence items as a workspace hold queue with workflow-state filters`,
  `GET ${apiRoutes.workspaceSystemCheckEvidenceBreachQueue("demo-tenant", "demo-workspace")}?groupKey=group-alpha&escalationTarget=audit-ops&breachQueueStatus=escalated&assignedToActorId=audit-primary lists escalation-relevant held evidence items as a dedicated breach queue with workflow-state filters`,
  `GET ${apiRoutes.workspaceSystemCheckEvidenceBreachDeadLetterQueue("demo-tenant", "demo-workspace")}?groupKey=group-alpha&status=pending_acknowledgement&deliveryChannel=email_spike lists delivery-failed breach notifications as a dedicated dead-letter queue with provider and workflow-state filters`,
  `GET ${apiRoutes.workspaceSystemCheckEvidenceBreachNotifications("demo-tenant", "demo-workspace")}?groupKey=group-alpha&escalationTarget=audit-ops&status=pending_acknowledgement&deliveryStatus=delivered&assignedToActorId=audit-primary lists persisted worker-created breach notifications together with notification-service-driven outbound delivery state, provider receipt metadata, policy-driven provider selection, and retry results as a workspace notification outbox`,
  `GET ${apiRoutes.workspaceSystemCheckEvidence("demo-tenant", "demo-workspace", "{evidenceKey}")} returns workspace-scoped evidence metadata plus a short-lived retrieval grant while payload bytes are still available, regardless of whether the evidence is stored in the configured S3-compatible backend or a legacy inline backend`,
  `POST ${apiRoutes.workspaceSystemCheckEvidenceHold("demo-tenant", "demo-workspace", "{evidenceKey}")} applies an operator retention hold that can promote evidence from workspace-review retention into the longer operator-investigation retention class while seeding acknowledgement, default assignment, escalation target, and SLA workflow state from the effective hold-reason policy`,
  `POST ${apiRoutes.workspaceSystemCheckEvidenceAssignHold("demo-tenant", "demo-workspace", "{evidenceKey}")} assigns a held evidence item to an operator and persists assignment workflow metadata on the hold itself`,
  `POST ${apiRoutes.workspaceSystemCheckEvidenceAcknowledgeHold("demo-tenant", "demo-workspace", "{evidenceKey}")} acknowledges a held evidence item when the effective hold-reason policy requires explicit acknowledgement`,
  `POST ${apiRoutes.workspaceSystemCheckEvidenceBreachNotificationAcknowledge("demo-tenant", "demo-workspace", "{notificationId}")} acknowledges a persisted breach notification independently from the underlying hold state after notification-service delivery`,
  `POST ${apiRoutes.workspaceSystemCheckEvidenceBreachNotificationRedrive("demo-tenant", "demo-workspace", "{notificationId}")} redrives a dead-letter breach notification back into pending delivery with an optional corrected target and a fresh provider-specific retry budget`,
  `POST ${apiRoutes.workspaceSystemCheckEvidenceReleaseHold("demo-tenant", "demo-workspace", "{evidenceKey}")} releases an operator retention hold so expired evidence can be purged on the next worker sweep`,
  `GET ${apiRoutes.workspaceSystemCheckEvidenceRetentionHistory("demo-tenant", "demo-workspace", "{evidenceKey}")} projects captured, hold-applied, hold-assigned, hold-acknowledged, worker-driven hold-escalated, hold-released, and purged retention lifecycle events for one evidence item`,
  `GET ${apiRoutes.systemCheckEvidenceAccess("{accessToken}")} resolves a short-lived evidence access token into the stored payload and preview content across both the configured S3-compatible backend and the legacy inline fallback`,
  `POST ${apiRoutes.workspaceSystemCheckLaunchApprovals("demo-tenant", "demo-workspace")} creates a stored workspace launch approval for a warning-level participant session and assignment`,
  `GET ${apiRoutes.workspaceSystemCheckLaunchApprovals("demo-tenant", "demo-workspace")}?participantSessionId={participantSessionId}&assignmentKey={assignmentKey}&status=active lists stored launch approvals for workspace review and reuse`,
  `POST ${apiRoutes.workspaceSystemCheckLaunchApprovalRevoke("demo-tenant", "demo-workspace", "{launchApprovalId}")} revokes an active stored launch approval with supervisor metadata and audit traceability`,
  `stored launch approvals can derive expiresAt from effective workspace launch-approval policy or carry explicit expiresAt timestamps, and appear as status=expired once their lifetime elapses`,
  `existing inheriting workspaces reflect tenant operational-policy, launch-approval-policy, notification-policy, evidence-retention-policy, and evidence-retention-class-policy changes live, while overridden workspaces keep only their explicitly overridden fields and continue inheriting the rest`,
  `GET ${apiRoutes.workspaceAuditEvents("demo-tenant", "demo-workspace")} lists persisted workspace audit events in reverse chronological order`,
  `GET ${apiRoutes.workspaceMonitorCommands("demo-tenant", "demo-workspace")}?testRunId={testRunId} lists persisted monitor commands with ack states for the selected run`,
  `GET ${apiRoutes.workspaceMonitorTestRuns("demo-tenant", "demo-workspace")}?groupKey=group-alpha lists open workspace runs for a specific monitor group`,
  `POST ${apiRoutes.workspaceSourcePackages("demo-tenant", "demo-workspace")} stores an immutable source-package stub`,
  `POST ${apiRoutes.workspaceImportJobs("demo-tenant", "demo-workspace")} queues an import job for a workspace source package`,
  `unknown source packages can fail import cleanly while valid jobs in the same workspace still produce a content release`,
  `GET ${apiRoutes.workspaceImportJob("demo-tenant", "demo-workspace", "{importJobId}")} returns staged import diagnostics, related artifacts, and audit trail entries`,
  `import diagnostics expose importer, source-manifest, source-model, and canonical summary artifacts for completed imports`,
  `import diagnostics expose source-to-canonical reference mappings for importer-backed source entities`,
  `import diagnostics expose a first-class failure object with failed stage, failure message, structured validation issues, mapping keys, and failure event metadata for failed imports`,
  `import diagnostics preserve useful artifacts when imports fail during source-model or canonical validation`,
  `GET ${apiRoutes.workspaceContentReleases("demo-tenant", "demo-workspace")} lists canonical content releases created by the worker together with comparison summaries, field-level change reasons, and activation-focused impact rollups against the next older workspace release`,
  `GET ${apiRoutes.workspaceContentRelease("demo-tenant", "demo-workspace", "{contentReleaseId}")} exposes canonical booklet, login, assignment, policy, previous-release diff detail with before/after field values, activation-focused impact rollups, and dynamic activation guardrails for a release`,
  `GET ${apiRoutes.workspaceContentReleaseMonitorProjection("demo-tenant", "demo-workspace", "{contentReleaseId}")} exposes a monitor-oriented projection of canonical groups, assignments, and booklets for operator planning`,
  `GET ${apiRoutes.workspaceContentReleaseSystemCheckProjection("demo-tenant", "demo-workspace", "{contentReleaseId}")} exposes a system-check-oriented projection of canonical checks, groups, and login coverage for operator planning`,
  `the importer registry can materialize more than one canonical fixture family in the same workspace`,
  `POST ${apiRoutes.contentReleaseActivate("demo-tenant", "demo-workspace", "{contentReleaseId}")} activates the selected release inside the workspace scope when activation guardrails are satisfied`,
  `release guardrails warn on high-risk switches once another release is active, even before any participant sessions are open`,
  `release guardrails block activation when incompatible routing changes would strand active participant sessions, and the blocked response includes the guardrail payload`,
  `relaxing the workspace activation policy can downgrade a previously blocked release switch into a warning and allow activation to proceed later in the same session lifecycle`,
  `POST ${apiRoutes.participantAuthSignIn} rejects sign-in when the target workspace has no active content release`,
  `POST ${apiRoutes.participantAuthSignIn} authenticates a participant against the active content release`,
  `GET ${apiRoutes.participantStarter}?participantSessionToken={token} resolves starter assignments from canonical content`,
  `GET ${apiRoutes.participantSystemCheck}?participantSessionToken={token} resolves canonical system-check definitions and prior structured submissions from the session-bound content release`,
  `POST ${apiRoutes.participantSystemCheckEvidenceCapture} stores participant-captured system-check evidence through the configured storage backend, emits an audit event, and returns a short-lived retrieval grant`,
  `expired evidence retention can purge non-inline payload bytes in the worker while preserving evidence metadata, surfacing payloadAvailability=purged in workspace review and rejecting older access tokens with system_check_evidence_purged, while active retention holds keep otherwise-expired evidence available until release, operator-investigation holds can move evidence into a longer retention class, and breached hold SLAs can be escalated by the worker into the workspace hold queue, the dedicated breach queue, a dead-letter queue for permanently failed outbound notifications, and a persisted workspace breach-notification outbox that the notification service delivers through the shared outbound-messaging webhook/email adapters with provider receipts, retry semantics, and manual redrive support`,
  `POST ${apiRoutes.participantSystemCheckSubmit} persists a participant system-check submission with richer per-check payloads, validates referenced evidence ownership, derives summary status, and emits an audit event`,
  `POST ${apiRoutes.workspaceSystemCheckResultReview("demo-tenant", "demo-workspace", "{systemCheckSubmissionId}")} persists a workspace review decision with note and reviewer provenance`,
  `participant starter responses expose launch readiness derived from the latest system-check submission and review outcome for each canonical system check`,
  `POST ${apiRoutes.participantStarterLaunch} blocks launch when system-check readiness is blocked and includes the readiness payload in the error details`,
  `warning-level system-check readiness requires a stored workspace launch approval before launch can continue`,
  `accepted warning-level system-check reviews can proceed to launch only when a matching launchApprovalId is supplied, while starter and launch responses still surface warning-level readiness`,
  `new warning-level system-check submissions can automatically invalidate older active launch approvals when readiness drops back to blocked or the warning reason codes change`,
  `expired launchApprovalId values are rejected on later launch attempts even if no other readiness change occurred`,
  `revoked or invalidated launchApprovalId values are rejected on later launch attempts even if the participant session remains in warning-level readiness`,
  `POST ${apiRoutes.participantStarterLaunch} creates or resumes a test run from a starter assignment once readiness is no longer blocked`,
  `GET ${apiRoutes.participantTestRun("{testRunId}")}?participantSessionToken={token} returns the session-bound test-run state`,
  `POST ${apiRoutes.participantTestRunSave("{testRunId}")} persists a response for the current unit`,
  `POST ${apiRoutes.participantTestRunNextUnit("{testRunId}")} advances the run after the current unit has been saved`,
  `POST ${apiRoutes.participantTestRunNavigate("{testRunId}")} rejects navigation while the run remains locked`,
  `POST ${apiRoutes.workspaceMonitorTestRunPause("demo-tenant", "demo-workspace", "{testRunId}")} enqueues a pause command that the dispatcher later applies to freeze the countdown`,
  `reissuing the same monitor command can still produce a persisted rejected command after queued delivery instead of silently mutating state`,
  `POST ${apiRoutes.workspaceMonitorTestRunResume("demo-tenant", "demo-workspace", "{testRunId}")} enqueues a resume command that the dispatcher applies without losing the attempt`,
  `POST ${apiRoutes.workspaceMonitorTestRunUnlock("demo-tenant", "demo-workspace", "{testRunId}")} enqueues an unlock command that the dispatcher later uses to open participant navigation for the targeted run`,
  `POST ${apiRoutes.participantTestRunNavigate("{testRunId}")} allows backward navigation after monitor unlock within the reachable unit set`,
  `mutation responses expose x-request-id headers that can be traced through workspace audit events`,
  `monitor commands create explicit dispatch-queue entries so delivery infrastructure is separated from command history`,
  `queued monitor command audit events reuse the original request id through issued, delivered, applied, or rejected outcomes`,
  `worker audit events distinguish completed and failed import jobs, while activation is visible in workspace audit history`,
  `the looping worker can persist timed-out test runs instead of relying only on lazy read-time timeout evaluation, and that maintenance can diverge across workspaces based on effective operational-policy grace`,
  `workspace activation-policy override records preserve x-request-id-linked provenance for each overridden rule`,
  `workspace operational-policy override records preserve x-request-id-linked provenance for each overridden rule`,
  `the looping dispatcher can expire stale unresolved monitor commands and expose them through ackState=expired using the effective workspace operational policy instead of only process-wide defaults`,
  `resume_or_create creates a new attempt after a worker-timed-out run instead of reusing the expired attempt`,
  `resume_or_create creates a new attempt after a completed run instead of reusing the finished attempt`,
  `npm run test:contracts executes the migrated end-to-end flow against a fresh local Postgres instance via Node's built-in TypeScript stripping for the large harness file`,
  `fixture catalog contains at least ${fixtureCatalog.length} importer-backed fixture families`
] as const;
